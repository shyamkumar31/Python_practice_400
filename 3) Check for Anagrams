Problem: Check for Anagrams
Description:
Write a program that takes two strings as input and determines if they are anagrams of each other. Two strings are anagrams if they contain the same characters in the same frequency, but possibly in a different order.

Input:
str1: A string containing only lowercase alphabetic characters (1 ≤ len(str1) ≤ 100).
str2: A string containing only lowercase alphabetic characters (1 ≤ len(str2) ≤ 100).

Output:
Return True if str1 and str2 are anagrams of each other, otherwise return False.

Examples:
if 'listen' and 'silent' are anagrams should return True.
if 'rat' and 'car' are anagrams should return False.

code:-

str1=input()
str2=input()
a=set(str1)
b=set(str2)
if a==b:
    print(True)
else:
    print(False)
